{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    // Other compiler options...

    "baseUrl": ".", // This should point to the root directory of your project
    "paths": {
      "@/*": ["src/_components/*"],
      "#/*": ["src/layouts/*"],
    }
  }
}